# Your admission controller. Read README.md first, then write plan.md,
# then implement here (any language is fine — if you switch languages, note
# the run command in plan.md).
#
# Protocol reminder: read stdin line by line, answer every DONE with
# zero or more ADMIT lines followed by END, and flush stdout after END.