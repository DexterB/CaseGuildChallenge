[project]
name = "caseguildchallenge"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "debugpy>=1.8.22",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
